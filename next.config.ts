import type { NextConfig } from "next";
// next.config.js
module.exports = {
  devIndicators: {
    appIsrStatus: false,   // disables the floating "N" symbol
  },
};


const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
